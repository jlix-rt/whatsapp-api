{
  "name": "whatsapp-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para WhatsApp Platform",
  "scripts": {
    "install:api": "cd apps/api && npm install",
    "install:inbox": "cd apps/inbox && npm install",
    "install": "cd apps/inbox && npm install && cd ../api && npm install",
    "build:api": "cd apps/api && npm run build",
    "build:inbox": "cd apps/inbox && npm run build",
    "start:api": "cd apps/api && npm start",
    "start:inbox": "cd apps/inbox && npm start"
  }
}
